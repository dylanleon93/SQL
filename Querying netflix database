-- In this SQL Code I am querying a database with Netflix data to answer questions.

-- How Many Movies are there in the database
SELECT Count(title) FROM "Database/Directory1"."netflix_titles_info" 
WHERE type = 'Movie';

-- When was the most recent batch of tv shows and/or movies added to the database? 

SELECT MAX(date_added) FROM "Database/Directory1"."netflix_titles_info";

-- Arrange all shows and movies in alphabetical order.

SELECT title FROM "Database/Directory1"."netflix_titles_info"
ORDER BY title asc;

-- Who was the Director for the movie "Close Enemies"?

SELECT director, title FROM "Database/Directory1"."netflix_titles_info"
LEFT JOIN "Database/Directory1"."netflix_people" 
ON netflix_people.show_id = netflix_titles_info.show_id
WHERE title = 'Close Enemies';

--What is the oldest movie in the database and what year was it made? 

Select title, release_year
FROM "Database/Directory1"."netflix_titles_info"
WHERE release_year <= (SELECT MIN(release_year) 
FROM "Database/Directory1"."netflix_titles_info"
WHERE type = 'Movie')
AND type = 'Movie'
;
